{
  "name": "searching-contextual",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "vite",
    "dev:server": "fuser -k 3001/tcp >/dev/null 2>&1 || true; pnpm --prefix server run dev",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "axios": "^1.15.2",
    "bottleneck": "^2.19.5",
    "csv-parse": "^6.2.1",
    "d3-force": "^3.0.0"
  }
}
